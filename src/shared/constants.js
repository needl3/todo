const modes = {
  LOGIN: "login",
  REGISTER: "register",
  DORMANT: "dormant",
};

const status = {
  REGISTERED: "registered",
  NOT_REGISTERED: "not-registered",
  NO_ATTEMPT: "",
};

export { modes, status };
